{
  "name": "task-management-dashboard",
  "version": "1.0.0",
  "description": "Task Management Dashboard with React, Redux Toolkit, and drag-and-drop",
  "private": true,
  "dependencies": {
    "@reduxjs/toolkit": "^2.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-redux": "^9.0.4",
    "react-beautiful-dnd": "^13.1.1",
    "axios": "^1.6.2",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server --watch db.json --port 5000",
    "server:prod": "node server.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

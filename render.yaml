services:
  - type: web
    name: task-dashboard-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /tasks
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
